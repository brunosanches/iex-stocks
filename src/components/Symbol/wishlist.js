import React from 'react'

import { ListBox } from './styles'

const Wishlist = ({ wishlist }) => (
  <ListBox>
    <img src="http://via.placeholder.com/180x55?text=2" />
  </ListBox>
)

export default Wishlist
